import QueueForm from '../components/QueueForm';

const HomePage = () => (
    <div>
        <h1 className="text-xl font-bold mb-4">Join the Queue</h1>
        <QueueForm />
    </div>
);

export default HomePage;
